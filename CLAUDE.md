# 코딩할 때 AI가 지킬 규칙

이 문서는 AndysNote에서 AI(Claude)와 함께 코딩할 때 지켜야 할 실무 규칙을 담는다.
프로젝트의 전체 철학과 모듈별 책임은 [.claude/CLAUDE.md](.claude/CLAUDE.md)와
[docs/PRINCIPLE.md](docs/PRINCIPLE.md)를 따른다. 이 문서는 그 철학을 실제 코딩
과정에서 어떻게 적용할지에 대한 세부 규칙과, 진행하면서 쌓이는 결정 사항을
기록하는 곳이다.

---

## 0. AI의 기본규칙
세션 내에서 코딩하면, 반드시 적절한 커밋 메시지와 함께 커밋을 진행한다.

## 1. 기존 구조 존중, 불필요한 리팩터링 금지

기존의 구조를 존중하며, 불필요하거나 잦은 리팩터링을 금지한다.

- 요청받지 않은 스타일 변경, 파일 이동, 이름 변경을 하지 않는다.
- "더 나은 방식이 떠올랐다"는 이유만으로 기존 코드를 고치지 않는다.
- 리팩터링이 필요하다고 판단되면 먼저 이유를 설명하고 승인을 받은 뒤 진행한다.
  (참고: 아래 [리팩토링 허용 기준](#리팩토링-허용-기준))

## 2. 목적·의도 충돌 시 반드시 보고

기존 기능이 가진 목적과 의도, 기능과 충돌하는 코딩을 할 때는 반드시 보고한다.

- 요청받은 구현이 기존 기능의 동작이나 데이터 형식(.txt/.md 저장, Google Drive
  동기화 등)을 깨뜨릴 가능성이 있다면 코드를 작성하기 전에 먼저 알린다.
- "이렇게 구현하면 A 기능이 깨질 수 있다"는 식으로 구체적인 충돌 지점을 짚어준다.
- 충돌이 사소해 보여도 임의로 판단해서 넘어가지 않는다.

## 3. 불확실하거나 개발자 결정이 필요한 사안은 즉시 질문

이외에도 확실하지 않거나 개발자의 결정이 필요한 사안이 발생하면 즉시 질문한다.

- 여러 구현 방식 중 트레이드오프가 있는 경우, 임의로 하나를 고르지 않고 먼저 묻는다.
- 데이터 구조 변경, 저장 방식 변경, UI/UX 방향처럼 되돌리기 어려운 결정은 특히
  신중하게 확인한다.
- 질문할 때는 선택지와 각각의 장단점을 간단히 제시해서 결정을 돕는다.

## 4. 복잡성 관리 규칙 준수

모듈화, 계층화, 추상화 등의 복잡성을 다루는 규칙을 기억하고 코딩한다.

- 새 기능은 [.claude/CLAUDE.md](.claude/CLAUDE.md) 3장의 모듈 책임 분리
  (Editor / Markdown Engine / Renderer / Storage / Settings)를 넘나들지 않는다.
- 하나의 파일은 하나의 책임만 가진다. 책임이 애매하면 구현 전에 어느 모듈이
  담당할지부터 정한다.
- 당장 필요하지 않은 미래 확장을 대비한 추상화(설정 플래그, 범용 헬퍼 등)를
  미리 만들지 않는다.

## 5. 개발자 결정 사항 기록

코딩을 하면서 개발자가 결정하는 것들을 이 파일 아래([결정 로그](#결정-로그))에
적는다. 이후 코딩에서 이 결정을 참고한다.

- 기능 하나를 구현하는 중에도 방향이 갈리는 지점이 있으면 사용자에게 묻고,
  답변받은 결정을 결정 로그에 날짜와 함께 기록한다.
- 이미 기록된 결정과 상충하는 요청이 들어오면, 새로 진행하기 전에 상충 사실을
  먼저 알린다.

## 6. 죽은 코드·불필요한 코드 제거

죽은 코드, 불필요한 코드를 반드시 제거하고 관련 코드를 효율적으로 관리한다.

- 기능을 교체하거나 제거할 때 이전 구현을 주석 처리해서 남겨두지 않는다.
- 사용하지 않는 변수, 함수, import는 확인 후 삭제한다.
- `console.log`, 임시 디버그 코드, 방치된 TODO를 남기지 않는다.
  (참고: [.claude/CLAUDE.md](.claude/CLAUDE.md) 금지사항 항목과 동일)

---

## 리팩토링 허용 기준

다음에 해당할 때만 리팩터링을 제안한다 ([.claude/CLAUDE.md](.claude/CLAUDE.md) 기준과 동일):

- 중복 코드가 늘어나고 있다.
- 유지보수가 눈에 띄게 어려워졌다.
- 가독성이 크게 향상된다.
- 버그 발생 가능성이 줄어든다.

단순히 스타일 취향 차이로는 리팩터링하지 않는다.

---

## 결정 로그

코딩하면서 사용자가 내린 결정을 아래에 날짜순으로 기록한다.
각 항목은 "무엇을 결정했는지"와 "왜 그렇게 결정했는지"를 함께 남긴다.

<!--
형식 예시:
### 2026-08-07 — 결정 제목
- 결정: (무엇을 하기로 했는지)
- 이유: (왜 그렇게 결정했는지)
- 영향: (영향을 받는 모듈/파일)
-->

### 2026-08-07 — 결정 사항은 CLAUDE.md에 기록
- 결정: 개발자 결정 사항(결정 로그)은 별도 `docs/DECISIONS.md`를 만들지 않고
  이 `CLAUDE.md` 파일의 결정 로그 섹션에 계속 기록한다.
- 이유: `AGENTS.md`가 `docs/DECISIONS.md`를 참조하고 있었지만 실제 파일은
  없었고, 사용자가 `CLAUDE.md`로 일원화하기로 결정함.
- 영향: `AGENTS.md`의 "Read First" 항목을 `docs/DECISIONS.md` 대신
  `CLAUDE.md` 결정 로그를 참조하도록 수정함.

### 2026-08-08 — AndysLetter 추가: Supabase는 우체국이지 문서 저장소가 아니다
- 결정: 유저 간 편지 기능 "AndysLetter"를 라이브러리·캘린더와 나란한 세 번째
  최상위 뷰로 추가한다. 편지 배달을 위해 Supabase(Postgres + Auth)를 도입한다
  (이 프로젝트의 첫 외부 서버·외부 JS 라이브러리 의존성).
- 이유: 원칙 2("모든 문서는 .txt/.md로 저장, 메모장으로 읽을 수 있어야")는
  사용자가 작성한 문서 데이터에 대한 것이다. Supabase에 있는 편지는 아직
  배달 중인 우편물이지 사용자의 문서가 아니라고 해석한다. 편지가 실제
  문서(Source of Truth)가 되는 시점은 Drive 또는 로컬로 "내보내기"할 때이며,
  그 순간 반드시 사람이 읽을 수 있는 `.md`로 저장된다(js/letter/export.js:
  letterBuildMarkdown). 이 해석으로 원칙 2와 AndysLetter는 충돌하지 않는다.
- 추가 결정:
  - 편지 도착 지연 없음(즉시 도착). 지연 배송은 만들지 않음(`docs/additional.md`
    로 이동).
  - 가입은 구글 로그인 + 이메일/비밀번호 둘 다 허용하되, 소규모 운영을 위해
    관리자(admin) 승인을 받아야 편지를 주고받을 수 있다(`letter_users.status`).
    관리자 계정 지정은 비밀번호를 코드/SQL에 남기지 않고 Supabase 대시보드에서만
    설정한다 (`docs/supabase-schema.sql` §7).
  - 내보내기 후 원본 처리: 구글 드라이브 연동 사용자는 Drive 저장이 확인된
    후에만 Supabase 원본을 삭제한다(무료 용량 절약). 구글 미연동 사용자는
    기존 로컬 노트로 저장하되 Supabase 원본은 남긴다 — 로컬 저장은 그 기기
    에만 있으므로 유일한 사본을 지우지 않기 위함.
  - `Letters` 폴더는 플래너의 `Calendar` 폴더와 달리 사이드바에서 숨기지
    않는다 — 내보낸 편지는 일반 문서이므로 라이브러리·캘린더·검색에 그대로
    잡히는 것이 맞다.
- 영향: `js/ui.js`의 `switchView`가 `library`/`calendar` 2분기에서
  `library`/`calendar`/`letter` 3분기로 바뀜. `js/config.js`, `js/state.js`,
  `js/i18n.js`에 AndysLetter 관련 블록 추가. 새 파일:
  `js/letter.js`, `js/letter/api.js`, `js/letter/papers.js`,
  `js/letter/compose.js`, `js/letter/admin.js`, `js/letter/export.js`,
  `docs/supabase-schema.sql`, `docs/additional.md`.
